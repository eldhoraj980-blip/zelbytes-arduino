# Overview

This project implements a manual irrigation system using Arduino.

When the operator presses and holds the push button, the relay energizes and opens the solenoid valve. Water flows only while the button remains pressed. The status LED indicates valve state and all irrigation events are logged through Serial Monitor with timestamps generated using millis().

This project serves as the Phase 1 Capstone by combining concepts from previous tasks into a single firmware application.
